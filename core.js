function scrollOverview () {
    var element = document.getElementById("overview");
    element.scrollIntoView({behavior: "smooth", block: "start"});
}

function scrollInstallation () {
    var element = document.getElementById("installation");
    element.scrollIntoView({behavior: "smooth", block: "start"});
}

function scrollUsage () {
    var element = document.getElementById("usage");
    element.scrollIntoView({behavior: "smooth", block: "start"});
}

function scrollCodeDocs () {
    var element = document.getElementById("code-docs");
    element.scrollIntoView({behavior: "smooth", block: "start"});
}

function scrollDevContact () {
    var element = document.getElementById("dev-contact");
    element.scrollIntoView({behavior: "smooth", block: "start"});
}